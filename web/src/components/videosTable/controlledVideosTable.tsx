import {Table} from "antd";
import VideosTable from "./videosTable";
import {useEffect, useState} from "react";
import useAPI from "../../hooks/useAPI";

export default function ControlledVideosTable() {
    // const [keywords, setKeywords] = useState<string>();
    // const [selected, setSelectedKeyword] = useState<string>();
    // let {keywordResults, keywordLoading} = useAPI({path:"/keywords"});
    // // let {data, dataLoading, getData} = useAPI("/keywords");
    //
    // useEffect(()=>{
    //     console.log(keywordResults);
    // },[keywordResults])


    return (
        <>PUPA</>
    );

}